{
  "name": "simarium",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-first autonomous terrarium simulation",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "headless": "npm run build --silent && node dist/tools/headless.js"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
